{
  "name": "@recoveryos/api-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node -e \"throw new Error('DORMANT: use Supabase Edge Functions; see workers/api/README.md')\"",
    "deploy": "node -e \"throw new Error('DEPLOY BLOCKED: this prototype overlaps canonical Supabase Edge Functions')\"",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250410.0",
    "typescript": "^5.8.0",
    "wrangler": "4.14.0"
  },
  "description": "DORMANT provenance-only prototype; Supabase Edge Functions own public intake."
}
