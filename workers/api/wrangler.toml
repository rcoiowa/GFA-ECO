# RecoveryOS shared API gateway (api.vrcc.app).
# Secrets (SUPABASE_SERVICE_ROLE_KEY etc.) are set via `wrangler secret put`,
# never committed.
name = "recoveryos-api"
main = "src/index.ts"
compatibility_date = "2025-06-01"

[vars]
# Canonical RecoveryOS-Launch backend (CQCX). The public intake gateway writes
# ONLY to recoveryos.residence_application_intake here. The retired dev project
# must never receive new public submissions (enforced by check-no-retired-ref).
SUPABASE_URL = "https://cqcxvwoukyhxyokfwnjm.supabase.co"
