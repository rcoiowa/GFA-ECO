# Deploy Report — <worker> — <date>

Per directive §15: every deployment records exactly what shipped.

- **Worker / project:**
- **Domain(s):**
- **Branch:**
- **Exact commit:**
- **Changed since last deploy:** (files / summary)
- **Migration versions applied:** (or "none")
- **Environment/secret changes:** (names only, never values)
- **Tests run:** (CI run link / local)
- **Verification level reached:** IDENTIFIED / COMPILED / ROUTE-RESOLVED /
  AUTHENTICATED SMOKE-TESTED / STAGING VERIFIED / PRODUCTION VERIFIED
- **Staging result:**
- **Rollback:** (previous commit / deployment id / "re-point domain to X")
- **Production verification:** (route + observed result, or "n/a")
